<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item android:id="@+id/menu_delete"
        android:icon = "@drawable/ic_baseline_delete_24"
        android:title="Delete"
        app:showAsAction="always"/>


    <item android:id="@+id/menu_edit"
        android:icon = "@drawable/ic_baseline_edit_24"
        android:title="Edit"
        app:showAsAction="always"/>


    <item android:id="@+id/menu_add"
        android:icon = "@drawable/ic_baseline_add_circle_24"
        android:title="Add"
        app:showAsAction="always"/>

    <!-- app:showAsAction -->
    <!--always - will always show in the action bar -->
    <!--never - will never show, and therefore will be available through the overflow -->
    <!--ifRoom - only if there is sufficient space in the action bar, then it would show it -->
    <!--withText - will include the item's title in the action bar -->
    <!--collapseActionView - if this item has an action view associated with it, it will show -->
</menu>